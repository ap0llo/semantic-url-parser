<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- 
        Target both 
          - .NET Standard 2.0 (for maximum compatibility) and 
          - .NET 5 (to nullable warnings/errors as this version has nullable annotations in the BCL)
    -->
    <TargetFrameworks>netstandard2.0;net5.0</TargetFrameworks>
  </PropertyGroup>

</Project>
