<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- 
        Target both 
          - .NET Standard 2.0 (for maximum compatibility) and 
          - .NET Standard 2.1 (to nullable warnings/errors as this version already has nullable annotations in the BCL)
    -->
    <TargetFrameworks>netstandard2.0;netstandard2.1</TargetFrameworks>
  </PropertyGroup>

</Project>
